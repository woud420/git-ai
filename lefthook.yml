pre-commit:
  parallel: true
  commands:
    format-check:
      glob: "**/*.{rs,toml}"
      run: make format-check
    lint:
      glob: "**/*.{rs,toml}"
      run: make lint
    doc-check:
      glob: "**/*.{rs,toml}"
      run: make doc
