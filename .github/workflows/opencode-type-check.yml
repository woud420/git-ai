name: OpenCode Plugin Type Check

permissions:
  contents: read

on:
  push:
    branches: [main, master]
    paths:
      - 'agent-support/opencode/**'
  pull_request:
    paths:
      - 'agent-support/opencode/**'

jobs:
  type-check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: agent-support/opencode

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v6
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: agent-support/opencode/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run type check
        run: npm run type-check

      - name: Test plugin entrypoint and checkpoint hooks
        run: npm test
